# Issues & Fixes — Actionable Checklist

Sorted Critical → Low. See [audit-report.md](audit-report.md) for full narrative and evidence.

| # | Severity | Finding | File(s)/Location | Fix | Effort |
|---|----------|---------|-------------------|-----|--------|
| 1 | **Critical** | Hardcoded diagnostic bypass token (`rrs-7x2p9q`) grants unauthenticated read+write access to `fund-data.json` and `pending-deposits.json` (can delete deposit records) with no session/secret check. | [api/prices.js:30-59](api/prices.js:30) | Delete this `diag=dedupe` branch entirely (it's marked "TEMP" and its job is done). If similar one-off maintenance actions are needed again, gate them behind `isAdminRequest()` like every other mutating endpoint, never a static query-string token. | S |
| 2 | **Critical** | Every authenticated investor (not just admin) can read `fund-data.json` via `/api/data`, which includes every other investor's deposit dates, amounts, and full legal name. The client renders this on the investor-facing "Ownership" page for all users. | [api/data.js:19,56](api/data.js:19) (`READABLE` set), [index.html:3341-3406](index.html:3341) (`renderDeposits()`), [index.html:523-559](index.html:523) (non-admin-reachable page) | Split `fund-data.json` server-side into (a) fund-aggregate fields (positions, cash, NAV, total deposits) that stay in `READABLE`, and (b) a per-investor deposit view. Add a new endpoint/parameter (e.g. `GET /api/data?file=fund-data.json&scope=own`) that filters `deposits` to records matching the requesting session's investor key before returning, and change `renderDeposits()` to request/display only the caller's own deposits unless `currentUser.admin`. | M |
| 3 | **High** | `SYNC_SECRET` is reused both as the diagnostic/API bypass secret and as the HMAC signing key for session tokens. A leak of one purpose compromises the other (forging session tokens for any user, including admin). | [lib/auth.js:22-23](lib/auth.js:22) | Introduce a separate `SESSION_HMAC_SECRET` env var used only by `hmac()`/`issueToken()`/`verifyToken()`; keep `SYNC_SECRET` scoped to its own header-based use only. Rotate both after the split. | S |
| 4 | **High** | No audit log records *who* performed a mutating admin action (deposit allocation, investor edit, manual sync) or *when* — only whole-document "before" snapshots exist, and only for some write paths (`backupAndWrite` callers). Can't reconstruct "who changed what" for 17a-4-style recordkeeping. | [lib/store.js:56-62](lib/store.js:56) (`backupAndWrite`), all `api/*.js` mutating handlers | Add an `audit_log` table (`actor_email, action, target_doc, before_hash/diff, created_at`) written inside every mutating handler (`allocate-deposit.js`, `sync-investors.js`, `sync-data.js`, `import-ib-xml.js`, `restore-investors.js`) using the already-verified session/admin identity. Make writes to this table happen in the same transaction as the data mutation where practical. | M |
| 5 | **High** | No MFA for any account, including the admin account that can mutate financial records and allocate deposits. | [api/data.js:36-42](api/data.js:36) (login), [lib/auth.js](lib/auth.js) | Add TOTP-based MFA (e.g. `otplib`) required at minimum for the admin account; store a `mfaSecret` per user (env-config or a small dedicated table) and require a second factor before issuing a session token for `admin: true` users. | M |
| 6 | **High** | Session tokens are bearer, 30-day TTL, with no server-side revocation store — a leaked token stays valid for up to 30 days with no way to invalidate it (no logout-all-devices, no rotation on password change, and no password-change flow exists at all). | [lib/auth.js:20,26-43](lib/auth.js:20) | Add a minimal server-side session/version table (`user_email, min_valid_iat`) checked in `verifyToken()`; bump `min_valid_iat` on demand (admin action, or a future password-change flow) to invalidate all outstanding tokens for that user. Consider shortening TTL to 7 days with silent refresh. | M |
| 7 | **High** | No field-level encryption for sensitive JSONB documents (`investors.json` PII, `fund-data.json` deposit/financial data) — a leaked `DATABASE_URL` or DB-level compromise exposes everything in cleartext, and backups share the exact same access path/credential as production data. | [lib/store.js](lib/store.js) (all `readDoc`/`writeDoc`) | Adopt Neon's/Postgres's native at-rest encryption (confirm enabled — infra-level) as the baseline, and additionally encrypt the highest-sensitivity fields (mailing address, phone, nationality) at the application layer with a KMS-managed key before writing to `investors.json`, decrypting only for admin reads. | L |
| 8 | **Medium** | Password hashing uses PBKDF2-SHA256 (100k iterations) rather than a memory-hard KDF (argon2/scrypt/bcrypt) — acceptable but not best-practice for password storage. | [api/data.js:40](api/data.js:40), [lib/auth.js:14-18](lib/auth.js:14) | Migrate to `argon2id` (e.g. `node-argon2`) on next password rotation; keep PBKDF2 verification as a fallback for existing hashes during a transition window, re-hashing on successful login. | M |
| 9 | **Medium** | `GET /api/ib-pulls?pull=<id>` is unauthenticated by design, and snapshot IDs are sequential Postgres bigints — every historical IB pull (positions, cash, trades) is enumerable by incrementing the ID, with no auth at all. | [api/ib-pulls.js:18-22](api/ib-pulls.js:18), [lib/store.js:184](lib/store.js:184) | Add a random, non-guessable token per snapshot (e.g. a `uuid` column generated at insert) and require it in the URL instead of the raw sequential `id`; keep the endpoint unauthenticated for the "open in new tab" use case, but make the identifier unguessable. | S |
| 10 | **Medium** | No IB-data sanity/bounds check before trusting `totalValue` as the new authoritative NAV point — a garbled or partial IB response could silently mint an incorrect NAV for every investor. | [api/ib-data.js:150-154](api/ib-data.js:150), [lib/nav.js](lib/nav.js) | Add a guard in `appendNavPoint`/`parseNavXml`: reject (or flag for manual review instead of auto-writing) any `totalValue` that deviates more than a configurable threshold (e.g. 25%) from the prior day's value without a matching deposit/withdrawal explaining the delta. | S |
| 11 | **Medium** | No automated test suite exists for the NAV/unit-settlement math in `lib/nav.js`, despite it being the most financially consequential and subtle logic in the codebase (per-investor unit pricing, settlement-window matching, pending-cash neutrality). | [lib/nav.js](lib/nav.js) — no `test/` dir, no test runner in [package.json](package.json) | Add a unit test suite (e.g. Vitest/Node's built-in `node:test`) covering `computeTotalUnitsAtDate`, `buildUnitSchedule`, `fixIbDepositNavs`, `applyPendingToLatest`, `recomputeNavSeries` with fixture deposit/series data, including the documented edge cases (same-size market-swing collision, multi-day settlement lag, pending-cash fold-in). | M |
| 12 | **Medium** | Investor-controlled/IB-sourced fields (name, mailing address, deposit description) are inserted into HTML via template literals with no escaping, both in the SPA and in statement emails — no XSS today (inputs are semi-trusted), but no defense-in-depth if that trust boundary ever changes. | [index.html:3388](index.html:3388), [index.html:2518-2521](index.html:2518), [api/send-statements.js:271,362](api/send-statements.js:271) | Add a small `escapeHtml()` helper and apply it to every field interpolated into HTML template strings that ultimately originates from investor input or an external feed (IB description text especially). | S |
| 13 | **Medium** | Session token stored in `localStorage` (not an `httpOnly` cookie) — any future XSS becomes full 30-day account takeover, including admin. | [index.html:1931,1944](index.html:1931) | Move session storage to an `httpOnly`, `Secure`, `SameSite=Strict` cookie set by `/api/data` on login; adjust `dataFetch()`/`adminSecretHeader()` to rely on the cookie instead of manually attaching a bearer header. (Larger refactor — pair with item 6.) | L |
| 14 | **Medium** | No login rate-limiting/lockout on `POST /api/data {action:"login"}` — a script can brute-force the 3 known account passwords with no throttling in application code. | [api/data.js:36-42](api/data.js:36) | Add basic rate limiting (e.g. a small Neon-backed counter keyed by IP+email, or Vercel Edge Config/KV) — lock out or exponentially back off after N failed attempts per email per hour. | M |
| 15 | **Medium** | Backups (`snapshots` table) share the exact same DB credential and access path as live `documents` — no separate role or write-restricted credential for backup data. | [lib/store.js](lib/store.js) (single `sql` client for both tables) | Create a distinct Postgres role for the application with `INSERT`-only (no `UPDATE`/`DELETE`) grants on `snapshots`, and use a separate, more privileged role only for the rare restore path (`restore-investors.js`). | M |
| 16 | **Low** | No documented data retention/deletion policy — snapshots and documents accumulate indefinitely with no defined multi-year retention target or purge mechanism, and there's no account-closure/data-deletion flow for a departing investor. | Repo-wide (no retention logic anywhere) | Document a retention policy (align to a 6-year 17a-4-style floor for financial records) and, separately, decide/implement what happens to an LP's PII (not the financial ledger, which must be retained) if they exit the fund. | S (policy) / M (implementation) |
| 17 | **Low** | Legacy public Vercel Blob storage (`https://yt6mbeqqdx5ifzj3.public.blob.vercel-storage.com/...`) is referenced in the migration script; unclear whether the old sensitive blobs (`fund-data.json`, `investors.json`, etc.) were deleted after the Neon migration completed. | [scripts/migrate-blobs-to-neon.mjs:19](scripts/migrate-blobs-to-neon.mjs:19) | Manually verify (Vercel Blob dashboard) that all blobs under the old prefix have been deleted; if confirmed migrated and no longer needed, delete them and consider removing/archiving the migration script from the active codebase. | S |
| 18 | **Low** | No explicit security headers configured (CSP, HSTS, X-Frame-Options, X-Content-Type-Options) — relying entirely on Vercel platform defaults. | [vercel.json](vercel.json) (only defines `crons`, no `headers`) | Add a `headers` block to `vercel.json` setting `Strict-Transport-Security`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, and a CSP scoped to the app's actual script/style/connect sources. | S |
| 19 | **Low** | Statement/deposit-notification emails always go to `pablomontoyarobledo@gmail.com` only — LPs never receive their own statements by email (functional/product gap, not a security hole, but relevant to the fund's actual recordkeeping/communication obligations). | [api/ib-data.js:431](api/ib-data.js:431), [api/send-statements.js:444](api/send-statements.js:444) | Change the `to` field to the investor's own email (`investor.email`) for their statement, keeping Pablo on `cc` if desired for oversight. | S |
| 20 | **Low** | No password-change/reset flow exists anywhere — password rotation currently requires a manual code change to `lib/auth.js` and a redeploy. | [lib/auth.js](lib/auth.js) | Add an authenticated "change password" endpoint (requires current password) at minimum; a full self-service reset-via-email flow is a larger addition and lower priority given the 3-user scale. | M |
